# accsaber-ranking-script

In order to use the ranking script, change the variables in main.py. Change 'mapset_path' to the relative location of the mapset's directory, change 'diff_str' to the difficulty of the map, and change 'category' to the acc category you wish to use for the criteria check. 